'use client';import Link from 'next/link';import {motion} from 'framer-motion';import {Swiper,SwiperSlide} from 'swiper/react';import 'swiper/css';import {matches,leagues,teams,players,news} from './data';
export function Hero(){return <section className="section min-h-[78vh] flex items-center"><div className="max-w-5xl"><p className="text-brand font-black">LIVE NOW • 4K HLS • REAL-TIME DATA</p><h1 className="text-5xl md:text-8xl font-black leading-[.9]">Football streaming with cinematic speed.</h1><p className="text-xl text-white/65 mt-6 max-w-3xl">KFC STREAMS combines adaptive playback, premium subscriptions, live chat, fixtures, scores, highlights, watchlists and an admin-grade control plane.</p><div className="mt-8 flex flex-wrap gap-3"><Link className="btn btn-red" href="/live/match-1">Watch Live</Link><Link className="btn btn-ghost" href="/fixtures">View Fixtures</Link></div></div></section>}
export function MatchCard({m}){return <motion.article whileHover={{y:-8,scale:1.02}} className="card p-4"><div className="aspect-video rounded-2xl bg-gradient-to-br from-red-700/50 to-zinc-950 grid place-items-center"><span className="text-5xl">⚽</span></div><p className="text-brand mt-4 text-sm font-bold">{m.league}</p><h3 className="text-xl font-black">{m.home} vs {m.away}</h3><div className="mt-3 flex justify-between text-white/65"><span>{m.score}</span><span>{m.time==='LIVE'?'LIVE':new Date(m.time).toLocaleDateString()}</span></div><Link href={`/live/${m.id}`} className="btn btn-red mt-4 w-full">Open Match</Link></motion.article>}
export function Rail({title,items=matches}){return <section className="section"><div className="mb-5 flex justify-between"><h2 className="text-3xl font-black">{title}</h2><Link href="/search" className="text-brand font-bold">Explore all</Link></div><Swiper spaceBetween={16} slidesPerView={1.1} breakpoints={{700:{slidesPerView:2.4},1100:{slidesPerView:4.2}}}>{items.map(m=><SwiperSlide key={m.id}><MatchCard m={m}/></SwiperSlide>)}</Swiper></section>}
export function Directory({type}){let arr=type==='leagues'?leagues:type==='players'?players:teams;return <section className="section"><h1 className="text-5xl font-black capitalize">{type}</h1><div className="grid-auto mt-8">{arr.map((x,i)=><Link href={`/${type}/${i+1}`} className="card p-6" key={x}><div className="text-4xl mb-6">🏆</div><b>{x}</b><p className="text-white/55 mt-2">Stats, fixtures, form, news and streaming availability.</p></Link>)}</div></section>}
export function InfoPage({title}){return <section className="prose-page"><h1 className="text-5xl font-black">{title}</h1><p className="mt-5 text-white/65">Production-ready {title.toLowerCase()} experience for KFC STREAMS with responsive UI, secure API integration, SEO metadata, notifications, skeleton loading, watch controls and premium account workflows.</p></section>}
export function NewsList(){return <section className="section"><h1 className="text-5xl font-black">News</h1><div className="grid-auto mt-8">{news.map(n=><article className="card p-6" key={n}><p className="text-brand font-bold">Breaking</p><h2 className="text-2xl font-black mt-2">{n}</h2><p className="text-white/60 mt-3">Editorial-ready article module with JSON-LD metadata and category routing.</p></article>)}</div></section>}
