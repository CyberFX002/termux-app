require('dotenv').config();const express=require('express'),http=require('http');const {Server}=require('socket.io');const security=require('./middleware/security');const routes=require('./routes');const app=express();security(app);app.use(express.json({limit:'1mb'}));app.use('/api',routes);app.get('/health',(req,res)=>res.json({ok:true,service:'kfc-streams-api'}));const server=http.createServer(app);const io=new Server(server,{cors:{origin:(process.env.CORS_ORIGIN||'http://localhost:3000').split(','),credentials:true}});require('./sockets')(io);server.listen(process.env.PORT||4000,()=>console.log('api listening'));
